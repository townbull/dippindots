# Convert a magnet link to a torrent file,
# place it into rtorrent's watch directory
[[ $1 =~ xt=urn:btih:([^&/]+) ]] || exit;
cd ~/.watch
fname="meta-${BASH_REMATCH[1]}.torrent"
echo "d10:magnet-uri${#1}:${1}e" > $fname